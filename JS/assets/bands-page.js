(()=>{const m=window.JOHN_SWAHN_MUSIC||{bands:[]},g=document.getElementById('bandGrid');if(!g)return;const root='../';g.innerHTML=m.bands.map((b,i)=>`<a class="band-card ${i%4===0?'wide':''}" href="${b.slug}.html">${b.artwork?`<img src="${root}${b.artwork}" alt="${b.name}" loading="lazy">`:'<div class="band-card-blank">JS</div>'}<div class="band-card-overlay"><span>${b.genre||''}${b.location?' · '+b.location:''}</span><h2>${b.name}</h2><p>${b.years} · ${b.releaseCount} skivor</p></div></a>`).join('');})();